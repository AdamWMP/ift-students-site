# Meta Pixel + Conversions API
# Public — exposed to the browser for client-side Pixel init
NEXT_PUBLIC_META_PIXEL_ID=974569781798246

# Server-only — never commit real values
META_PIXEL_ID=974569781798246
META_CAPI_ACCESS_TOKEN=replace_with_token_from_events_manager
# Remove META_TEST_EVENT_CODE (or leave empty) once verified and going live
META_TEST_EVENT_CODE=
